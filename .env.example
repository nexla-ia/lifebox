# Supabase — copie para .env.local e preencha.
# A anon key e publica (protegida por RLS). A service_role NUNCA vai para o front.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Usado so pelos scripts de migration/teste, fora do bundle.
DATABASE_URL=
